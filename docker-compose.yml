version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    image: dafnefirstapp
    container_name: dafneapp
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
      - ./logs:/app/logs
    restart: unless-stopped

